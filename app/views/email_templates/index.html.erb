<h1>Listing email_templates</h1>

<table>
  <tr>
    <th>Course</th>
    <th>Price</th>
    <th>Title</th>
    <th>Topics</th>
    <th>Highlight</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @email_templates.each do |email_template| %>
  <tr>
    <td><%= email_template.course %></td>
    <td><%= email_template.price %></td>
    <td><%= email_template.title %></td>
    <td><%= email_template.topics %></td>
    <td><%= email_template.highlight %></td>
    <td><%= email_template.content %></td>
    <td><%= link_to 'Show', email_template %></td>
    <td><%= link_to 'Edit', edit_email_template_path(email_template) %></td>
    <td><%= link_to 'Destroy', email_template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Email template', new_email_template_path %>
